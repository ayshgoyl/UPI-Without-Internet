server:
  port: ${PORT:8085}
  ssl:
    enabled: ${SSL_ENABLED:false}
    key-store: classpath:keystore.p12
    key-store-password: password
    key-store-type: PKCS12
    key-alias: qrcodeservice

spring:
  application:
    name: qr-payment-service
